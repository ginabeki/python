# debugging, the act of removing errors from our code
# good practices
# linting, allows us to detect some issues with our code
# always use IDE/ editor
# read errors
# pdp, python debugger
import pdb
def add(num1, num2):
    pdp.set_trace()
    return num1 + num2
add(4, 'dgsgds')
